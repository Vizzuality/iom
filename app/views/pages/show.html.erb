<h1>Pages#show</h1>

<h2><%= @page.title %></h2>

<div class="main">
<%= @page.body %>
</div>

<div class="sidebar">
  <% unless @page.children.empty? %>
  <ul>
    <% @page.children.each do |page| %>
    <li><%= link_to(page.title, page_path(page)) %></li>
    <% end %>
  </ul>
  <% end %>
</div>