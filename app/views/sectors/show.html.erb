  
  <div class="main_head">
    <div class="inner_main_head">
      <div class="left">
        <h2><%= @site.word_for_sectors %></h2>
        <h1><%= @sector.name %></h1>
        <div class="float_head">
          <h2><%= @site.word_for_sectors %></h2>
          <h1><%= @sector.name %></h1>
          <span class="water"></span>
        </div>
      </div>
      <div class="right">
        <p>Texto para el sector. Esto no aparece en el back de administración porque no hay gestión de clusters, así que a ver de dónde lo sacamos.</p>
      </div>
    </div>
  </div>



  <div id="map"></div>

  <div id="layout">
    <div id="left_column">
  
      <!-- LEFT PART -->
      <div class="left"></div> <!-- H"J"ACK -->
      <div class="outer_float">
        <div class="float_left">
          <div class="head">
            <h2>FACTSHEET</h2>
          </div>

          <div class="block statistics">
            <h3>PROJECTS BY LOCATION</h3>
              <ul class="chart">
              <% index = 0%>
              <% values = '' %>
              <% max_value = 0 %>
              <% @sector.projects_regions(@site).each do |region, count| %>
                <li class="pos<%= index %>"><a href="#"><%= truncate(region.name,:length=>15, :omission=>"...") %></a> - <%= count %></li>
                <% values += ((index==0) ? '' : ',') + count.to_s() %>
                <% max_value<count ? max_value=count : nil %>
                <% index = index + 1 %>
              <% end %>
            </ul>
            <img class="pie_chart" src="http://chart.apis.google.com/chart?cht=p&chs=120x120&chd=t:<%= values %>&chds=0,<%= max_value %>&chco=333333|565656|727272|ADADAD|EFEFEF|FFFFFF&chf=bg,s,FFFFFF00" />
          </div>
        
        
          <div class="block">
            <h3>DONORS</h3>
            <ul>
              <% @sector.donors(@site).each do |donor| %>
                <li><%= link_to(donor.name, donor_path(donor)) %></li>
              <% end %>
            </ul>
          </div>

  
          <div class="block margin">
            <h3>OTHER CLUSTERS</h3>
            <ul class="clusters">
            <% (@site.sectors - @sector.to_a).each do |sector| %>
              <!-- TODO -> Choose kind of clusters -->
              <li class="water">
                <%= link_to(sector.name, sector_path(sector)) %>
                <p>X projects</p>
              </li>
            <% end %>
            </ul>
          </div>

        </div>
      </div>

      <!-- RIGHT PART -->
      <div class="right">
        <h3><%= @sector.name.upcase %> LIST OF PROJECTS</h3>
        <div id="projects">
          <%= render :partial => 'projects/projects' %>
        </div>
        <div class="view_more">
          <a id="view_more">More projects</a>
        </div>
      </div>
    </div>

    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"></script> 
    <script src="/javascripts/sites/location.js" type="text/javascript" charset="utf-8"></script>