  <!-- div id="header_sites" -->
  <div id="gallery">
    <div class="content_gallery">
      <% @sites.each do |site| %>
        <%= link_to(image_tag(site.aid_map_image.url(:huge), :title => site.try(:name), :desc => truncate(site.short_description, :length => 72), :alt => truncate(site.long_description, :length => 278)), url(site)) %>
      <% end %>
    </div>
  </div>
  <!-- <div id="gallery"> -->
  <ul id="gallery_thumbails">
    <% @sites.each_with_index do |site, i| %>
    <li class="<%= ((i + 1)%4==0) ? 'last' : '' %>">
        <div id="image">
          <%= link_to(image_tag(site.aid_map_image.url(:small), :alt => site.name), url(site)) %>
        </div>
        <a class="site_name" href="<%= url(site) %>"><%= site.name %></a>
      </li>
    <% end %>


    <li class="<%= ((@sites.size+1)%4==0) ? 'last' : '' %>">
        <div id="image">
          <img src="/images/sites/home/more_project.jpg" alt="More projects soon"/>
        </div>
        <a class="site_name">MORE PROJECTS SOON!</a>
    </li>
  </ul>

<script src="/javascripts/sites/home_sites.js" type="text/javascript" charset="utf-8"></script>
<script src="/javascripts/plugins/galleria.js" type="text/javascript" charset="utf-8"></script>
<script src="/javascripts/plugins/galleria.home.js" type="text/javascript" charset="utf-8"></script>