<div class="main_layout">
  <div class="block first donor_funded">
    <div class="top"></div>
    <div class="med">
      <div class="left">
        <h2><%= @donor.name %></h2>
        <p><%= pluralize(@donor.donated_projects.count, ' funded project', 'funded projects') %></p>
        <% unless @donor.donated_projects.empty? %>
          <ul>
            <% @donor.donated_projects.each do |project| %>
            <li>
              <span class="first">
                <h3><%= link_to(project.name, edit_admin_project_path(project)) %></h3>
                <p class="<%= project.finished? ? 'closed' : 'active' %>"><%= project.finished? ? 'Closed' : 'Active' %></p>
              </span>
              <p>$12,000</p>
            </li>
            <% end -%>
          </ul>
        <% end -%>
      </div>
      <div class="right menu">
        <%= render 'admin/donors/sidebar' %>
      </div>
    </div>
    <div class="bottom"></div>
  </div>
  
  <script src="/javascripts/backoffice/application.js" type="text/javascript" charset="utf-8"></script>