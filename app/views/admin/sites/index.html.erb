<h2>Sites</h2>

<p><%= link_to('+ new site', new_admin_site_path) %></p>

<ul>
  <% @sites.each do |site| %>
    <li><%= link_to(site.name, edit_admin_site_path(site)) %></li>
  <% end -%>
</ul>

<%= will_paginate(@sites) %>