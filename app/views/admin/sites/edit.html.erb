<div class="main">

  <h2>Edit site <%= @site.name %></h2>

  <%= render :partial => (action_name == 'edit' ? 'form' : 'form_customization') %>

</div>

<div class="sidebar">
  <%= render 'sidebar' %>
</div>