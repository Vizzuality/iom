<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%=raw(javascript_include_merged(:base)) %>
  <%=raw(javascript_include_merged(:base_2)) %>
  <%=raw(stylesheet_link_merged(:base)) %>
  <%=stylesheet_link_tag @site.theme.css_file if @site.theme %>
  <link rel="shortcut icon" href="/images/favicon.ico"/>
</head>
<body>

  <% unless action_name == 'home' && controller_name == 'sites' %>
    <!-- div id="header" -->
    <%= render :partial => 'shared/menu' %>
  <% end %>

  <div id="outer_layout">

    <%= yield %> <!-- Main layout has to content <div id="layout"> -->

    <!-- FOOTERS -->
    <% if controller_name == 'pages' || controller_name == 'search' || controller_name == 'donors' || controller_name == 'projects' %>
      <div id="footer_pages">
        <div class="left">
          <a href="http://www.interaction.org/" class="logo"></a>
          <div class="project">
            <h1><a href="/">NGOAIDMAP</a></h1>
            <p class="all_rights">Copyrighted by InterAction ©2011. All rights reserved.</p>
          </div>
        </div>
        <div class="right">
          <ul>
            <li><a href="/p/contact">contact us</a></li>
            <li>/</li>
            <li><a href="/p/about">About the project</a></li>
          </ul>
        </div>
      </div>
    <% else %>
      <div id="footer">
        <div class="left">
          <a href="http://www.interaction.org/" class="logo"></a>
        </div>
        <div class="right">
          <div class="project">
            <h1><%= @site.name %></h1>
            <p class="all_rights">Copyrighted by InterAction ©2011. All rights reserved.</p>
          </div>
          <div class="links">
            <ul>
              <li><a href="<%= page_path(Page.contact(@site)) %>">contact us</a></li>
              <li>/</li>
              <li><a href="<%= page_path(Page.about(@site)) %>">About the project</a></li>
            </ul>
          </div>
        </div>
      </div>
    <% end %>
    </div>
  </div>
  <%= yield :js %>
</body>
</html>